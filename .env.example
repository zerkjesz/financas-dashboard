# DATABASE_URL local aponta pro branch de DEV/TEST do Neon, NUNCA pro branch de
# produção (main). Veja docs/dev-environment.md pra como criar esse branch e por quê.
# Produção só existe como variável de ambiente na Vercel (Project Settings ->
# Environment Variables -> Production) — nunca num arquivo .env local.
DATABASE_URL="postgresql://usuario:senha@ep-XXXXXXXX-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Fase 5.4F — DIRECT_URL estava faltando aqui (achado no leak/env-contract
# scan final), apesar de já ser exigida por prisma/schema.prisma
# (`directUrl = env("DIRECT_URL")`) e já configurada no .env local de
# desenvolvimento. `prisma migrate`/`db push` precisam de uma conexão DIRETA
# (sem pgbouncer/connection pooling) pra funcionar corretamente contra o
# Postgres do Neon — é a MESMA connection string de DATABASE_URL, só sem o
# sufixo "-pooler" no host (ex: ep-XXXXXXXX.sa-east-1.aws.neon.tech, sem
# "-pooler"). O app em runtime (queries normais) usa só DATABASE_URL.
DIRECT_URL="postgresql://usuario:senha@ep-XXXXXXXX.sa-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Declaração explícita de que este ambiente NÃO é produção. Scripts de teste
# (scripts/lib/assertTestEnvironment.js) exigem "development" ou "test" aqui pra
# aceitar escrever qualquer dado — sem essa variável, eles abortam (fail closed).
DATABASE_ENV=development

# Token do bot no Telegram (@BotFather)
TELEGRAM_TOKEN=seu_token_aqui

# Secret que o Telegram envia de volta no header X-Telegram-Bot-Api-Secret-Token do
# webhook — configurado via setWebhook(..., secret_token=...). Ver plano de segurança
# em docs/schema-v2-blueprint.md, item 12.
TELEGRAM_WEBHOOK_SECRET=

# Fase 5.3C.1, item 26 — renomeado de OWNER_CHAT_ID: o valor aqui é o from.id
# (identidade do USUÁRIO do Telegram que envia a mensagem/aperta o botão),
# nunca um chat.id — os dois coincidem numericamente numa conversa privada
# 1:1 por particularidade do Telegram, mas são conceitos diferentes (ver
# lib/auth/telegramSecurity.js:isAuthorizedTelegramSender). Descobrir o seu:
# mande qualquer mensagem pro bot uma vez e confira o campo
# update.message.from.id do update recebido (ou peça pro @userinfobot).
TELEGRAM_ALLOWED_USER_ID=

# Autenticação do dashboard web (ver docs/schema-v2-blueprint.md, item 11,
# implementada na Fase 5.3C — lib/auth/**, middleware.js).
# DASHBOARD_PASSWORD_HASH: saída de `node scripts/generate-password-hash.mjs "sua senha"`
# — formato "scrypt:N:r:p:saltHex:hashHex" (":" e não "$" — o loader de env do
# Next.js interpola "$NOME" mesmo dentro de aspas simples, o que corromperia
# um hash no formato $ tradicional). NUNCA a senha em texto puro.
DASHBOARD_PASSWORD_HASH=

# SESSION_SECRET: 32+ bytes aleatórios (64+ chars hex), independente da senha
# — gerar com `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`.
SESSION_SECRET=

# Fase 5.3C, item 24 — bypass de auth (web + Telegram) SOMENTE pra
# desenvolvimento local, NUNCA default, NUNCA aceito em produção mesmo se
# setado por engano (middleware.js/lib/auth/envConfig.js travam isso
# explicitamente). Deixe vazio/ausente pra continuar exigindo login/allowlist
# também em dev (preferível).
AUTH_DEV_BYPASS=

# Fase 5.3D.1 — timezone IANA usada pra resolver "hoje"/"ontem"/dia da semana
# em linguagem natural (Telegram) na data-calendário LOCAL, nunca a data UTC
# crua do servidor (ver lib/appTimezone.js). Não é segredo — é config de
# apresentação, mesma categoria de pt-BR/BRL já hardcoded no app. Deixe
# vazio/ausente pra usar o default (America/Sao_Paulo).
APP_TIMEZONE=

# Fase 7.0.1, item 1 — liga/desliga o pipeline conversacional (lib/telegramAi/**)
# como uma escolha operacional EXPLÍCITA, separada de "tem ANTHROPIC_API_KEY?".
#   false (ou ausente, default seguro) -> pipeline NUNCA é chamado, 100% parser legado.
#   true  -> pipeline é SEMPRE chamado; qualquer falha dali pra frente (sem
#            chave, timeout, erro, resposta malformada) é zero write + mensagem
#            de segurança — NUNCA cai pro parser legado nesse caminho.
TELEGRAM_AI_ENABLED=false

# Fase 7.0 — credencial do interpretador semântico via Anthropic Messages API
# (lib/telegramAi/llmProvider.js). Sem ela, o pipeline (quando
# TELEGRAM_AI_ENABLED=true) sempre responde "não consegui interpretar... zero
# write" — nunca inventa uma chave, nunca escreve com uma interpretação pior.
# Gere em https://console.anthropic.com/settings/keys — NUNCA commitar o valor real.
ANTHROPIC_API_KEY=

# Fase 7.0.2, item 2 — id do modelo usado pelo interpretador semântico. NUNCA
# hardcoded no código (lib/telegramAi/llmProvider.js não tem fallback nenhum
# pra esta variável) — sem ela (ou com ANTHROPIC_API_KEY ausente), o pipeline
# falha fechado exatamente como "sem provider configurado", nunca escolhe
# outro model sozinho. Valide o id real contra a API antes de confiar nele
# (scripts/telegram-ai-real-provider-smoke.mjs faz exatamente isso).
ANTHROPIC_MODEL=
