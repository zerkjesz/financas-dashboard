node_modules/
.next/
.env
*.db
*.db-journal
prisma/generated/
.vercel
.env*
prisma/backup-*.json

# Fase 5.0 — dados financeiros reais do usuário NUNCA versionados.
scripts/snapshot-input.local.json
scripts/snapshot-reports/

# Fase 5.3A — targets canônicos reais (valores financeiros pessoais), usados só
# em runtime pelo acceptance harness read-only, nunca versionados.
scripts/fase53a-targets.local.json
scripts/lib/.bootstrap-authorized.json
